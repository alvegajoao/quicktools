<UserControl x:Class="QuickTools.Views.PowerSchedulerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:services="clr-namespace:QuickTools.Services">
    <UserControl.Resources>
        <Style x:Key="EventRowStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="10,9" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="InlineButtonTemplate" TargetType="Button">
            <Border x:Name="Root"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="10"
                    Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  TextElement.Foreground="{TemplateBinding Foreground}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Root" Property="Opacity" Value="0.82" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="InlineToggleStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template" Value="{StaticResource InlineButtonTemplate}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Title]}" Style="{StaticResource PageTitleStyle}" />
        <TextBlock Grid.Row="1" Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Subtitle]}"
                   Style="{StaticResource MutedTextStyle}"
                   Margin="0,0,0,24" />

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,16,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="58"
                            Height="58"
                            CornerRadius="18"
                            Background="{DynamicResource AccentSurfaceBrush}"
                            BorderBrush="{StaticResource AccentBrush}"
                            BorderThickness="1"
                            Margin="0,0,16,0">
                        <TextBlock Text="&#xE916;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontSize="24"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock Text="{Binding OverallStatus}" FontSize="30" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding NextEventLabel}" Style="{StaticResource MutedTextStyle}" Margin="0,6,0,0" />
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_PauseAll]}"
                        Command="{Binding PauseAllCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Height="48"
                        MinWidth="140"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,0,18" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_ScheduleEvent]}" Style="{StaticResource SectionTitleStyle}" />

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Action]}" Style="{StaticResource MutedTextStyle}" />
                        <ComboBox ItemsSource="{Binding ActionOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Value" SelectedValue="{Binding SelectedAction}" />
                    </StackPanel>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Hour]}" Style="{StaticResource MutedTextStyle}" />
                            <ComboBox ItemsSource="{Binding Hours}" SelectedItem="{Binding SelectedHour}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Minute]}" Style="{StaticResource MutedTextStyle}" />
                            <ComboBox ItemsSource="{Binding Minutes}" SelectedItem="{Binding SelectedMinute}" />
                        </StackPanel>
                    </Grid>

                    <Button Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Schedule]}"
                            Command="{Binding AddEventCommand}"
                            HorizontalAlignment="Stretch"
                            Height="42" />

                    <Border Background="{DynamicResource WarningBackgroundBrush}"
                            BorderBrush="{DynamicResource WarningBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="12,8"
                            Margin="0,14,0,0">
                        <TextBlock Foreground="{DynamicResource WarningTextBrush}"
                                   FontSize="12"
                                   Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_ConfirmationNotice]}" />
                    </Border>

                    <TextBlock Text="{Binding Message}" Style="{StaticResource MutedTextStyle}" Margin="0,10,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,16,18" VerticalAlignment="Top">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_Title]}" Style="{StaticResource SectionTitleStyle}" Margin="0" />
                        <TextBlock Text="{Binding EventsCountLabel}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Bottom" />
                    </Grid>

                    <Border Visibility="{Binding EmptyVisibility}"
                            Background="{DynamicResource SurfaceAltBrush}"
                            CornerRadius="12"
                            Padding="20,24">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="&#xE916;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="30"
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerScheduler_NoScheduledEvents]}"
                                       Style="{StaticResource MutedTextStyle}"
                                       HorizontalAlignment="Center"
                                       Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>

                    <ItemsControl ItemsSource="{Binding Events}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource EventRowStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0"
                                                Width="44"
                                                Height="44"
                                                CornerRadius="14"
                                                Background="{DynamicResource AccentSurfaceBrush}"
                                                BorderBrush="{StaticResource AccentBrush}"
                                                BorderThickness="1"
                                                Margin="0,0,12,0">
                                            <TextBlock Text="{Binding Icon}"
                                                       FontFamily="Segoe MDL2 Assets"
                                                       FontSize="20"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center" />
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayAction}" FontWeight="SemiBold" FontSize="14" />
                                            <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                <TextBlock Text="{Binding TimeLabel}" Style="{StaticResource MutedTextStyle}" FontSize="12" />
                                                <TextBlock Text=" / " Style="{StaticResource MutedTextStyle}" FontSize="12" />
                                                <TextBlock Text="{Binding DateLabel}" Style="{StaticResource MutedTextStyle}" FontSize="12" />
                                            </StackPanel>
                                        </StackPanel>

                                        <Button Grid.Column="2"
                                                VerticalAlignment="Center"
                                                Margin="0,0,6,0"
                                                Command="{Binding DataContext.ToggleEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Content="{Binding ToggleLabel}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource InlineToggleStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Grid.Column="3"
                                                Width="34"
                                                Height="34"
                                                Cursor="Hand"
                                                VerticalAlignment="Center"
                                                Command="{Binding DataContext.RemoveEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Transparent" />
                                                    <Setter Property="BorderThickness" Value="0" />
                                                    <Setter Property="Padding" Value="4" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="Root"
                                                                        Background="{TemplateBinding Background}"
                                                                        CornerRadius="8"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Root" Property="Background" Value="{DynamicResource DangerSubtleBrush}" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                            <TextBlock Text="&#xE74D;"
                                                       FontFamily="Segoe MDL2 Assets"
                                                       FontSize="13"
                                                        Foreground="{DynamicResource DangerSoftTextBrush}" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
