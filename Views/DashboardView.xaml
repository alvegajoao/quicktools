<UserControl x:Class="QuickTools.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:converters="clr-namespace:QuickTools.Converters"
              xmlns:services="clr-namespace:QuickTools.Services">
    <UserControl.Resources>
        <converters:GaugeNeedleAngleConverter x:Key="GaugeNeedleAngleConverter" />

        <Style x:Key="DashboardTileStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="MinHeight" Value="176" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,14,14" />
        </Style>

        <Style x:Key="MetricPillStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="14,12" />
        </Style>

        <Style x:Key="DashboardIconBoxStyle" TargetType="Border">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="42" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Background" Value="{DynamicResource AccentSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="MiniIconStyle" TargetType="Border">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="{DynamicResource AccentSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,7,0" />
        </Style>

        <Style x:Key="SwitchPillStyle" TargetType="Border">
            <Setter Property="Width" Value="56" />
            <Setter Property="Height" Value="30" />
            <Setter Property="CornerRadius" Value="15" />
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="SwitchThumbStyle" TargetType="Border">
            <Setter Property="Width" Value="22" />
            <Setter Property="Height" Value="22" />
            <Setter Property="CornerRadius" Value="11" />
            <Setter Property="Background" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="4,0,0,0" />
        </Style>

        <Style x:Key="MiniTableRowStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
            <Setter Property="CornerRadius" Value="9" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style x:Key="ModeStackButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="11,7" />
            <Setter Property="Margin" Value="0,0,0,7" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Title]}" Style="{StaticResource PageTitleStyle}" />
        <TextBlock Grid.Row="1" Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Subtitle]}"
                   Style="{StaticResource MutedTextStyle}"
                   Margin="0,0,0,24" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource DashboardTileStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Style="{StaticResource DashboardIconBoxStyle}">
                            <TextBlock Text="&#xE7C9;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="21"
                                       Foreground="{StaticResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource SwitchPillStyle}">
                            <Grid>
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource SwitchThumbStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAutoClickerRunning}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                                    <Setter Property="Margin" Value="0,0,4,0" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.ColumnSpan="2">
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_AutoClicker]}" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="0" Margin="0,38,12,0">
                            <TextBlock Text="{Binding AutoClickerSpeedLabel}"
                                       FontSize="28"
                                       FontWeight="SemiBold"
                                       TextWrapping="NoWrap" />
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Speed]}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="12,38,0,0">
                            <TextBlock Text="{Binding AutoClickerElapsedTime}"
                                       FontSize="28"
                                       FontWeight="SemiBold"
                                       TextWrapping="NoWrap" />
                            <TextBlock Text="{Binding AutoClickerHotkeyLabel}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource DashboardTileStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Style="{StaticResource DashboardIconBoxStyle}">
                            <TextBlock Text="&#xE8A7;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="21"
                                       Foreground="{StaticResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource SwitchPillStyle}">
                            <Grid>
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource SwitchThumbStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsQuickToggleActive}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                                    <Setter Property="Margin" Value="0,0,4,0" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="1" Margin="0,14,0,0">
                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_QuickToggle]}" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding QuickToggleSummary}"
                                   FontSize="21"
                                   FontWeight="SemiBold"
                                   Margin="0,4,0,0" />
                        <ItemsControl ItemsSource="{Binding QuickToggleActions}" Margin="0,10,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource MiniIconStyle}" ToolTip="{Binding Name}">
                                        <TextBlock Text="{Binding Icon}"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   FontSize="14"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource DashboardTileStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="168" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <Border Style="{StaticResource DashboardIconBoxStyle}" Margin="0,0,0,14">
                            <TextBlock Text="&#xE945;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="21"
                                       Foreground="{StaticResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_PowerMode]}" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding CurrentPowerPlan}"
                                   FontSize="21"
                                   FontWeight="SemiBold"
                                   Margin="0,4,16,0"
                                   TextWrapping="Wrap" />
                        <TextBlock Text="{Binding PowerPlanSummary}"
                                   Style="{StaticResource MutedTextStyle}"
                                   Margin="0,8,16,0" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button Command="{Binding SetBalancedCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModeStackButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsBalancedActive}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE9D2;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Margin="0,0,7,0" />
                                 <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerPlan_Balanced]}" />
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding SetHighPerformanceCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModeStackButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsHighPerformanceActive}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE945;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Margin="0,0,7,0" />
                                 <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerPlan_HighPerformance]}" />
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding SetPowerSaverCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModeStackButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPowerSaverActive}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE83F;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Margin="0,0,7,0" />
                                 <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerPlan_PowerSaver]}" />
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding SetUltimateCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModeStackButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUltimateActive}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xEC4A;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Margin="0,0,7,0" />
                                 <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[PowerPlan_UltimatePerformance]}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource DashboardTileStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="0" />
                        </Grid.ColumnDefinitions>

                        <Border Style="{StaticResource DashboardIconBoxStyle}">
                            <TextBlock Text="&#xE916;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="21"
                                       Foreground="{StaticResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                    </Grid>

                    <StackPanel Grid.Row="1" Margin="0,14,0,10">
                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_PowerScheduler]}" FontSize="16" FontWeight="SemiBold" />
                        <ItemsControl ItemsSource="{Binding ActiveSchedulerEvents}" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource MiniTableRowStyle}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="54" />
                                                <ColumnDefinition Width="68" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding DisplayAction}" FontWeight="SemiBold" FontSize="12" />
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding TimeLabel}"
                                                       FontWeight="SemiBold"
                                                       FontSize="12"
                                                       HorizontalAlignment="Center" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding DateLabel}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       FontSize="12"
                                                       HorizontalAlignment="Right" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <Button Grid.Row="2"
                            Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_PauseAllEvents]}"
                            Command="{Binding PauseAllEventsCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            HorizontalAlignment="Left" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource DashboardTileStyle}" MinHeight="300">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Style="{StaticResource DashboardIconBoxStyle}">
                            <TextBlock Text="&#xE9D9;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="21"
                                       Foreground="{DynamicResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_SystemPerformance]}" FontSize="16" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_SystemPerformanceSubtitle]}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,3,0,0" />
                        </StackPanel>

                        <Border Grid.Column="2"
                                Background="{DynamicResource AccentSurfaceBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="12,6"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Live]}"
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontSize="12"
                                       FontWeight="SemiBold" />
                        </Border>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*" />
                            <ColumnDefinition Width="18" />
                            <ColumnDefinition Width="1.15*" />
                            <ColumnDefinition Width="18" />
                            <ColumnDefinition Width="1.35*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource MetricPillStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_CPU]}" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                <Grid Grid.Row="1" Width="176" Height="118" HorizontalAlignment="Center" Margin="0,8,0,4">
                                    <Path Stroke="{DynamicResource BorderBrushSoft}" StrokeThickness="14" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 22 96 A 72 72 0 0 1 154 96" />
                                    <Path Stroke="{DynamicResource AccentBrush}" StrokeThickness="14" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 22 96 A 72 72 0 0 1 154 96" Opacity="0.35" />
                                    <Path Data="M 84 96 L 92 96 L 88 28 Z" Fill="{DynamicResource TextBrush}">
                                        <Path.RenderTransform>
                                            <RotateTransform CenterX="88" CenterY="96" Angle="{Binding SystemMetrics.CpuUsagePercent, Converter={StaticResource GaugeNeedleAngleConverter}}" />
                                        </Path.RenderTransform>
                                    </Path>
                                    <Ellipse Width="14" Height="14" Fill="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,15" />
                                </Grid>
                                <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding CpuUsageLabel}" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                    <TextBlock Text="{Binding CpuTemperatureDisplayLabel}" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource MetricPillStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_GPU]}" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                <Grid Grid.Row="1" Width="176" Height="118" HorizontalAlignment="Center" Margin="0,8,0,4">
                                    <Path Stroke="{DynamicResource BorderBrushSoft}" StrokeThickness="14" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 22 96 A 72 72 0 0 1 154 96" />
                                    <Path Stroke="{DynamicResource AccentBrush}" StrokeThickness="14" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 22 96 A 72 72 0 0 1 154 96" Opacity="0.35" />
                                    <Path Data="M 84 96 L 92 96 L 88 28 Z" Fill="{DynamicResource TextBrush}">
                                        <Path.RenderTransform>
                                            <RotateTransform CenterX="88" CenterY="96" Angle="{Binding SystemMetrics.GpuUsagePercent, Converter={StaticResource GaugeNeedleAngleConverter}}" />
                                        </Path.RenderTransform>
                                    </Path>
                                    <Ellipse Width="14" Height="14" Fill="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,15" />
                                </Grid>
                                <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding GpuUsageLabel}" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                    <TextBlock Text="{Binding GpuTemperatureDisplayLabel}" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid Grid.Column="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Style="{StaticResource MetricPillStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE950;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" FontSize="18" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Memory]}" FontWeight="SemiBold" />
                                        <ProgressBar Value="{Binding SystemMetrics.RamUsagePercent}" Maximum="100" Height="6" Margin="0,8,0,0" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding RamUsageLabel}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="2" Style="{StaticResource MetricPillStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xEDA2;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" FontSize="18" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Disk]}" FontWeight="SemiBold" />
                                        <ProgressBar Value="{Binding SystemMetrics.DiskUsagePercent}" Maximum="100" Height="6" Margin="0,8,0,0" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding DiskUsageLabel}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="4" Style="{StaticResource MetricPillStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE968;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" FontSize="18" VerticalAlignment="Center" />
                                    <Grid Grid.Column="1" Margin="12,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Download]}" Style="{StaticResource MutedTextStyle}" />
                                            <TextBlock Text="{Binding NetworkDownloadLabel}" FontSize="18" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Dashboard_Upload]}" Style="{StaticResource MutedTextStyle}" />
                                            <TextBlock Text="{Binding NetworkUploadLabel}" FontSize="18" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
